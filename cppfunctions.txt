#SORTING
sort(arr_name, arr_name+length, comp_function); -> in place
sort(arr_name, arr_name+length, less<int>()[optional]) ->ascending
sort(arr_name, arr_name+length, greater<int>()) ->descending

#COUNT
count(arr,arr+len,element)

#FIND
find(arr,arr+len,element) -arr;  //if element not found, returns size of array

#ARRAY EQUALITY
equal(arr1, arr1+len, arr2)-> bool value

#REVERSE ARRAY
reverse(nums,nums+len) -> in place reverse

~~~~~~~~VECTOR~~~~~~
#SWAP
swap(vec[1],vec[2]);

#COUNT
count(vec1.begin(),vec1.end(),element);

#ERASE
v.erase(vec1.begin()+index);

# ADD ELEMENT AT INDEX
v.insert(v.begin()+index,val);
v1.insert(v1.end(),v2.begin(),v.end());

#LOCATION
find(v.begin(),v.end(),element) - v.begin();

#REVERSE
reverse(v.begin(),v.end());

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~String~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

#conversion
(char)var
(int)var

#right rotation
string rr_s=s.substr(n-k,k)+s.substr(0,n-k);

#left rotation
string lr_s=s.substr(k,n-k)+s.substr(0,k);